// A program demonstrating arrays and for loops

int main() {
    int arr[3];
    arr[0] = 10;
    arr[1] = 20;
    arr[2] = 30;

    int sum = 0;
    for (int i = 0; i < 3; i = i + 1) {
        sum = sum + arr[i];
    }
    print(sum); // Outputs 60
}